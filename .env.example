# Learn English — Environment Variables
# Only GEMINI_API_KEYS is needed for AI features (text analysis, quiz generation)
# All other data is stored in the browser's localStorage — no database required

# ============ Gemini AI (REQUIRED for AI features) ============
# Get free keys at https://aistudio.google.com/apikey
# Multiple keys for rotation: comma-separated, no spaces
# Example: key1,key2,key3
GEMINI_API_KEYS=""
